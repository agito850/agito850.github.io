*(注意：前綴有底線 `_`，且副檔名是 `.yml`，不是 `.ymal`)*

3. **填入主題設定（以你想換 Jekyll 主題為例）**：
   在檔案內容中，根據你想使用的主題，填入對應的設定。例如，如果你想使用 GitHub Pages 內建的 `cayman` 主題，請輸入：
   ```yaml
remote_theme: pages-themes/hacker@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
